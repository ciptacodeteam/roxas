version: "2"
authtoken: ${NGROK_AUTHTOKEN}
web_addr: 0.0.0.0:4040

tunnels:
  app:
    proto: http
    addr: app:3000
    inspect: true
    schemes:
      - https
      - http
    host_header: rewrite
